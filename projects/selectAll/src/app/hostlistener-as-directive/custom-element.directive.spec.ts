import { CustomElementDirective } from './custom-element.directive';

describe('CustomElementDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomElementDirective();
    expect(directive).toBeTruthy();
  });
});
